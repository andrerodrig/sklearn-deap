cimport numpy as np
from sklearn_deap.individual.lib_individual cimport get_iterate_cx, get_iterate_mut


# def iterate_mut(list individual, list up, double indpb) -> list:
#     return lib_individual.get_iterate_mut(individual, up, indpb)

# def iterate_cx(list ind1, list ind2, double indpb, list gene_type) -> tuple:
#     return lib_individual.get_iterate_cx(ind1, ind2, indpb, gene_type)
